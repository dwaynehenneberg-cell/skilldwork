# Use temporary approval for workflow improvements

The current product uses Service Provider approval as a temporary safety gate before a suggested workflow version affects future Service Runs. The target product automatically applies safe improvements, so the public workflow visualization omits this transitional approval detail. The embedded Revision Agent may adapt the current Service Run but does not change future runs.
